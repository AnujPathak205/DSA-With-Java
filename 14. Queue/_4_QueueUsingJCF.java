import java.util.ArrayDeque;
import java.util.LinkedList;
import java.util.Queue;

public class _4_QueueUsingJCF {
    public static void main(String[] args) {
        // Queue<Integer> q = new LinkedList<>();
        Queue<Integer> q1 = new LinkedList<>();
        Queue<Integer> q2 = new ArrayDeque<>();
        /*
         * Methods
         * add(E e) → inserts, throws exception if fails

         *offer(E e) → inserts, returns false if fails

         *poll() → removes & returns head, returns null if empty

         *remove() → removes & returns head, throws exception if empty

         *peek() → reads head, returns null if empty

         *element() → reads head, throws exception if empty
         */

        
    }
}
